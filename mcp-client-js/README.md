# MCP on Windows Client sample

This sample shows how to interact as an MCP client on Windows. This sample lists all MCP servers registered on Windows and allows you to manually call a tool from one.

## Set up instructions

- Git clone this project
- Run `npm install` to install required packages
- Run `npm start` to start the app
