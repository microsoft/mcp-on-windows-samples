<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="mcp_server_msix.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:mcp_server_msix"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="MCP Server - Port 8000">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="API Demo Application" 
                   FontSize="20" 
                   FontWeight="Bold" 
                   Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <Button x:Name="QuoteButton" 
                    Content="Get Random Quote" 
                    Click="QuoteButton_Click" 
                    Margin="0,0,10,0"
                    Padding="15,10"/>
            <Button x:Name="WeatherButton" 
                    Content="Get Weather" 
                    Click="WeatherButton_Click" 
                    Margin="0,0,10,0"
                    Padding="15,10"/>
            <Button x:Name="FactButton" 
                    Content="Get Random Fact" 
                    Click="FactButton_Click" 
                    Padding="15,10"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2" 
                      x:Name="LogScrollViewer"
                      VerticalScrollBarVisibility="Auto">
            <TextBox x:Name="LogTextBox"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     Background="{ThemeResource LayerFillColorDefaultBrush}"
                     FontFamily="Consolas"
                     FontSize="12"/>
        </ScrollViewer>
    </Grid>
</Window>
